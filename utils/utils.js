const {database} = require('../database');

// exports.checkEmailExist = (email) => {
   
//     const checkIfExists = `SELECT email FROM users WHERE email = "${email}"`;

//         let final = database.query(checkIfExists, (err, result) => {
          
//             if (err){
//                 throw err
//             };

//              return result;
//         }
        
//         // return final;
     
//     }
   

